# 第 21 日完整成长验证样本

这里是隔离模拟生成的完整 Codex V2 图集：九类标准动作共 57 帧、16 个注视方向和一个静止参考格。使用内置 imagegen 生成，并用未修改的官方 hatch-pet 脚本提取、组装、清理和校验。`spritesheet.webp` 的摘要及验证范围见 `evidence.json`。

三份独立代理方向判读的多数结果通过全部 14 组检查。它们没有看到答案，但曾判读较早候选；这不是人类用户研究。`direction-semantics.json` 保留主代理逐方向判断及末端过渡不均匀警告。结构通过不代表画面完美。

此目录没有安装清单，不会增加原生 Pet 条目，也未覆盖真实宠物。真实宿主自动刷新、完整实时播放以及每日连续变化仍未验收。对照此前 `growth-day21-idle` 中的初生参考，可以检查同一角色更长的躯干比例。`generation-prompts.json` 保存本轮新增行的完整生成提示，先前 base/idle 提示保存在相邻 idle 样本证据中。
